import '../../scss/form.scss';

export const Form = () => {
  const handleChange = e => {
        
  };

  return (
    <form action="" className="search">
      <input className="search__field" type="text" placeholder="Filter by name..." />
            
    </form>
  );
};